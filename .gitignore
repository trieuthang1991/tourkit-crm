bin/
obj/
node_modules/
*.user
.vs/
appsettings.*.local.json

# Cấu hình thật của từng máy — chứa khoá API, chuỗi kết nối, khoá ký JWT.
# Mẫu để sao chép: src/TourKit.Api/appsettings.example.json (file này CÓ trong git).
src/TourKit.Api/appsettings.json

# SQLite dev database (tạo lại bằng `dotnet ef database update`)
*.db
*.db-shm
*.db-wal
.superpowers/
.playwright-cli/
aspnet-core-vue.zip

# Kiểm thử giao diện (tests/e2e)
tests/e2e/node_modules/
tests/e2e/test-results/
tests/e2e/playwright-report/
tests/e2e/package-lock.json

# Chỉ mục code graph — sinh cục bộ, ~100 MB, không commit (đã có tiền lệ gỡ khỏi git vì nặng).
.codegraph/
.codanna/
