@page
@model TourKit.Api.Pages.Notifications.BellModel
@* Trang KHÔNG có giao diện — chỉ là nơi đặt handler JSON cho chuông thông báo trên navbar.
   Navbar là partial dùng chung mọi trang nên không nạp được dữ liệu qua model của từng trang. *@
