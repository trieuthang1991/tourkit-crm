@* Dấu hiệu thương hiệu TourKit — ĐỊNH NGHĨA DUY NHẤT.
   Trước đây khối này chép tay ở 5 chỗ (menu dọc + 4 trang xác thực), sửa logo là phải nhớ
   sửa đủ 5 nơi, sót một chỗ là lệch thương hiệu. Giờ đổi ở đây là đổi hết.
   Model = lớp CSS cho phần chữ (menu và trang xác thực dùng cỡ chữ khác nhau). *@
@model string
<span class="app-brand-logo demo">
  <img src="~/img/branding/tourkit-mark.svg" alt="" width="24" height="24" />
</span>
<span class="app-brand-text demo @Model">TourKit</span>
