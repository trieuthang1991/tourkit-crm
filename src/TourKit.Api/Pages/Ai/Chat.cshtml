@page
@model TourKit.Api.Pages.Ai.ChatModel
@{
    // Trang này không có giao diện riêng: khung chat là một panel trượt do tk-ai.js dựng trên MỌI trang.
    // Ở đây chỉ có các handler JSON (?handler=Ask, ?handler=Status).
}
