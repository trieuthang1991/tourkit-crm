{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Dev KHÔNG gửi mail thật qua SES: ghi ra log (đổi thành "Smtp" nếu cần test gửi thật.
  // Prod dùng cấu hình Email ở appsettings.json).
  "Email": {
    "Provider": "Log"
  }
}
