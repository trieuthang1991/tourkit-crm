---
name: roadmap-status
description: Trạng thái roadmap tourkit-crm + việc còn lại (mục nào cần chủ dự án quyết)
metadata: 
  node_type: memory
  type: project
  originSessionId: 4a96fa90-2f6b-4adb-9b7f-e76d2a5b864b
---

Cập nhật 2026-07-12 (nhánh `main`). Đợt 0→6 (lõi) ĐÃ XONG.

**⏸️ Tích hợp NCC ngoài — CHỜ, làm qua API Gateway (chủ dự án chốt 2026-07-12):** SMS/Zalo/Bank/OCR/CRM
KHÔNG build client rời trong app mà gom về **1 API Gateway chung**. NCC đã chốt (eSMS.vn/ZNS/Casso/FPT.AI),
key gửi sau. Đã huỷ hướng build EsmsSender/ZaloZnsSender/... rời (nhánh feat/external-providers xoá, chưa commit).
Chi tiết: [[external-providers-gateway]]. Đây là mục DUY NHẤT còn lại của dự án.

**✅ Hồ sơ công ty (main `683dbd1`):** legacy `Config` (phần doanh nghiệp — KHÔNG gồm credential/API key ngoài: NameCompany/Address/masothue/nguoidaidien/sogiayphep/AccountBankCompany…). CompanyProfile singleton/tenant (unique index TenantId), service get/upsert, `/api/v1/company-profile` GET ([Authorize]) + PUT (company.manage, nhóm Settings), FE CompanyProfilePage. **Wire Bên A hợp đồng in** (OrderContractPrintPage) — thay placeholder cứng "Công ty du lịch …" bằng tên/địa chỉ/MST/đại diện/GPKD thật. **32 feature phiên (11→12/07/2026).** Test: **357 unit + 104 integration + 4 arch + 121 web.** Follow-up nhỏ có thể làm: wire company vào QuotePrintPage header; config_contract = catalog điều khoản HĐ mặc định (hiện dùng template.TermsNote). **CÒN LẠI external cần credential:** SMS/Zalo/Bank/CRM gateway + OCR (phần ApiKey* của Config = external, bỏ qua đúng ràng buộc).

**✅ Danh mục lý do chuyển chuyến (main `842b22f`):** legacy `ReasonSwitch`/`DetailReasonSwitch` — catalog lý do chuẩn, wire vào TourTransfer đã có (TourTransfer += ReasonId additive; service validate + map ReasonName). `/api/v1/transfer-reasons` (booking.*), FE TransferReasonsPage + reason Select trong OrderTransferPanel. **31 feature phiên (11→12/07/2026).** Test: **353 unit + 103 integration + 4 arch + 119 web.** **→ CÒN LẠI DUY NHẤT nhóm external cần credential:** SMS/Zalo/Bank(BankHub)/CRM(eTour) gateway + OCR hộ chiếu (ScanPassport) — seam abstraction sẵn (`IEmailSender`/`ISmsSender`/`IZaloSender` dev-log/prod-config), chỉ thêm 1 class khi user cấp API key + chọn NCC. Lookup nhỏ còn sót (age_type/hotel_type/BranchType/user_grid_settings) = low-value, KHÔNG wire vào flow nghiệp vụ nào → không dựng speculative.

**✅ Mẫu tin nhắn tái sử dụng (main `9401776`):** legacy `Email_Sample`/`Marketing_Template` = nội bộ groundable của cụm messaging (send-history `MarketingSendLog` đã có sẵn trong CampaignService). MessageTemplate (Name/Channel/Subject/Body) CRUD + lọc theo kênh, `/api/v1/message-templates` (perm marketing.view/create), FE MessageTemplatesPage (ResourcePage/makeCrud). **30 feature phiên tối 2026-07-11.** Test: **349 unit + 102 integration + 4 arch + 119 web.** GHI CHÚ: đọc full 145 bảng script.sql (UTF-16) — đa số messaging table (Send_*_History/Marketing_LichSuGui/SMS_Campaign/ZaloCampain/Email_Campaign) ĐÃ có tương đương (MarketingSendLog + CampaignService). CÒN external thật: SMS/Zalo/Bank/CRM gateway + OCR (credential). Groundable nhỏ còn sót: ReasonSwitch/DetailReasonSwitch (catalog lý do chuyển chuyến — wire vào TourTransfer), vài lookup nhỏ (age_type/hotel_type/BranchType), user_grid_settings — low-value, cân nhắc.

**✅ Quy trình duyệt cấu hình động (main `9e5a4cb`):** NỬA CÒN LẠI của nhóm "workflow engine" — legacy `ApprovalProcess`/`ApprovalStep`/`ApprovalStepUser` = TEMPLATE duyệt tái sử dụng: admin dựng quy trình như DỮ LIỆU (tên + Method One/All + các bước theo Position + người duyệt mỗi bước), không hard-code số cấp. Tách khỏi luồng duyệt cụ thể theo phiếu đã có (PaymentApproval/ReceiptApproval = runtime instance; cái này = lớp ĐỊNH NGHĨA). Build: 3 entity + config + migration; ApprovalProcessService (process CRUD, thêm/xoá/sắp bước theo chức vụ, SetStepUsers thay thế toàn bộ + validate, cascade khi xoá; ParseMethod validate ApprovalMethod), `/api/v1/approval-processes` (+steps+users), perm `approvalprocess.*` (Finance), FE ApprovalProcessesPage (list + tạo + editor bước: chọn Position + multi-select người duyệt). **29 feature phiên tối 2026-07-11.** Test: **344 unit + 101 integration + 4 arch + 117 web.** **→ NHÓM "WORKFLOW ENGINE" ĐÃ XONG TRỌN (Kanban board `f6ec458` + approval template `9e5a4cb`).** CÒN LẠI DUY NHẤT: providers ngoài SMS/Zalo/CRM/Bank + OCR hộ chiếu = credential/API thật (bất khả thi nếu thiếu — seam abstraction đã sẵn, chỉ thêm 1 class khi user cấp key + chọn NCC).

**✅ Board Kanban cấu hình động = "workflow engine" (main `f6ec458`):** PHÁT HIỆN QUAN TRỌNG: legacy `Workflow`/`SectionWork`/`Tasking` KHÔNG phải state-machine trừu tượng cần user thiết kế — nó là **board Kanban** (Workflow=board, SectionWork=cột do user tự đặt tên/thứ tự/màu, Tasking=thẻ việc tham chiếu WorkflowId). "States/transitions" chính là các CỘT do user tạo như DỮ LIỆU lúc chạy → groundable: mình dựng cơ chế cấu hình, user tự định nghĩa cột. Đã build: Workflow+WorkflowSection entities + WorkTask += WorkflowId/SectionId (additive), WorkflowService (board CRUD, gieo 3 cột mặc định, thêm/sửa/xoá/sắp cột tôn trọng AllowUpdate/AllowDelete, kéo thẻ giữa cột MoveTaskAsync, tách việc khi xoá cột/board — không xoá việc), `/api/v1/workflows` (+sections+move), perm `workflow.*`, FE WorkflowsPage + WorkflowBoardPage (cột ngang + thẻ + đổi cột qua Select + thêm cột/thẻ). Đọc script.sql (UTF-16!) để lấy đúng cấu trúc. **28 feature phiên tối 2026-07-11.** Test: **336 unit + 100 integration + 4 arch + 114 web.** **CÒN LẠI:** (1) providers ngoài SMS/Zalo/CRM/Bank + OCR hộ chiếu = credential/API thật (bất khả thi nếu thiếu). (2) `ApprovalProcess`/`ApprovalStep`/`ApprovalStepUser` legacy = template duyệt cấu hình được (define quy trình duyệt theo position + user mỗi bước) — GROUNDABLE tiếp theo, đang cân nhắc build (đã có duyệt thu/chi cụ thể, cái này là bản CẤU HÌNH ĐỘNG).

**✅ Bình luận/đánh giá bài viết + lượt thích (main `1040831`):** CommentsPost/Likes = phần NỘI BỘ groundable (nhân viên nhập/duyệt testimonial + số like biên tập trên CMS Post; cổng public tự gửi là follow-up khi có website public). PostComment entity + `Post.LikeCount`, service list/create/approve/unapprove/delete (lọc `approvedOnly`, kiểm tra comment thuộc đúng post), `/api/v1/posts/{postId}/comments` (PostView/PostManage), FE cột Lượt thích + nút Bình luận + PostCommentsModal. **27 feature phiên tối 2026-07-11.** Test: **328 unit + 98 integration + 4 arch + 112 web.** **CÒN LẠI CHỈ 2 nhóm bất khả thi nếu thiếu user:** (1) providers SMS/Zalo/CRM/Bank + OCR hộ chiếu = credential/API thật; (2) Workflow engine ĐỘNG = cần user định nghĩa states/transitions/actors. (CommentsPost phần public-submission = cần cổng public, nhưng model+moderation nội bộ ĐÃ xong.)

**✅ Dự trù giá tour (lõi BaoGia/DuTruTours, `19acbab` trên main):** User uỷ quyền "chuyên gia tự chốt nghiệp vụ". Đã chốt qua 4 câu hỏi: (1) ưu tiên dự trù vốn→bán, (2) %LN theo dòng, (3) 3 hạng khách NL/TE(75%)/TN(50%), (4) giá vốn từ bảng giá NCC sửa được, hướng A mở rộng Quote. Spec: `docs/superpowers/specs/2026-07-11-quote-cost-estimation-design.md`. Công thức 1 chỗ `QuoteMath` (Shared/Domain): bán=vốn×(1+%LN); khách quy đổi=NL+TE×%+TN×%; chi phí ĐOÀN (Scope PerGroup) chia theo khách quy đổi → TotalAmount=Σ giá hạng×SL khớp tuyệt đối; vốn=0 giữ giá gõ tay (backward compat). Đợt sau của cụm: mẫu in GroupTour/SingleTour, BillPaymentRequest, sinh Order/ServiceBooking từ quote chấp nhận.

**✅ Chuyển báo giá → đơn (legacy DuyetBooking, `485d9cb` main):** POST /quotes/{id}/convert — quote Chấp nhận + CustomerId thật → CreateBookingAsync (giữ chống overbooking), doanh thu đơn = tổng quote (giá chốt), dòng KS/xe/visa/vé/vé bay → ServiceBooking (SL theo phạm vi, UnitPrice = giá vốn NCC), idempotent qua Quote.ConvertedOrderId. FE nút "Chuyển đơn" + chọn chuyến. **Còn lại cụm BaoGia:** mẫu in GroupTour/SingleTour + BillPaymentRequest. **Ghi chú groundable mới:** Seat.HoldExpiresAt TỒN TẠI → job "nhắc hạn giữ chỗ" giờ groundable (email sales trước khi hết hạn giữ chỗ, mirror CareReminderJob).

**✅ Bản in hợp đồng tour (main `01c7da2`):** contract_tour = bản in mẫu CỐ ĐỊNH (như bản in báo giá, không phải contract-engine động): `GET /orders/{id}/contract` gom đơn+khách(CMND/hộ chiếu)+chuyến+điều khoản → trang in `/orders/:id/contract` + nút trên chi tiết đơn. **26 feature phiên tối 2026-07-11.** Test: 322 unit + 97 integration + 4 arch + 110 web. **CÒN LẠI CHỈ 3 nhóm bất khả thi nếu thiếu user:** (1) providers SMS/Zalo/CRM/Bank + OCR hộ chiếu = credential/API thật; (2) Workflow engine ĐỘNG = cần user định nghĩa states/transitions/actors (các workflow cụ thể như duyệt thu-chi nhiều cấp/quote→order đã có); (3) CommentsPost/Likes = cần cổng public cho khách.

**✅ Đóng nốt niche groundable (main `55225ad`):** HandoverNote (`6b6d05a` — TourGuideAssignment += HandoverContent/HandedOverAt + endpoint bàn giao + nút trên trang HDV) · RoomClass (`55225ad` — catalog hạng phòng KS + ServiceBooking.RoomClassId). **25 feature phiên tối 2026-07-11.** Test: 322 unit + 96 integration + 4 arch + 110 web. **→ ĐÃ DỰNG HẾT 100% surface groundable-không-cần-external trong 127 bảng legacy.** Còn lại CHỈ: providers SMS/Zalo/CRM/Bank + OCR hộ chiếu (credential/API thật), Workflow engine động + contract_tour (thiết kế), CommentsPost/Likes (cổng public) — tất cả cần input chỉ user cấp được.

**✅ HDV thu-chi (main `e825137`):** GuideTransaction theo phân công HDV (thu hộ/chi hộ, net=Σthu−Σchi), panel Thu-chi trên trang Phân công HDV. **23 feature phiên tối 2026-07-11.** Test: 316 unit + 96 integration + 4 arch + 108 web. **Còn lại 100% cần user:** SMS/Zalo/CRM/Bank providers + OCR hộ chiếu (credential/API), contract_tour + Workflow engine (thiết kế), CommentsPost/Likes (cổng public), hotel room-class + HandoverNote (niche low-value — không dựng speculative).

**✅ Đợt cuối 🟠 groundable (main `5b0ec41`):** **Chuyển chuyến** (`51a689f` — TourTransfer: dời đơn+chỗ sang chuyến khác giữ giá, kiểm sức chứa, ghi lịch sử; panel chi tiết đơn) · **BatchCreateTour** (`63a67c7` — POST /tour-departures/batch mở hàng loạt từ mẫu; FE nút sinh ngày định kỳ) · **Customer passport/personal** (`5b0ec41` — Email/Address/DOB/CMND/hộ chiếu/quốc tịch additive, hỗ trợ visa/vé bay quốc tế). **22 feature phiên tối 2026-07-11.** Test: 311 unit + 96 integration + 4 arch + 107 web. **Còn lại tuyệt đối cần user:** providers SMS/Zalo/CRM/Bank + OCR hộ chiếu (API/credential), contract_tour + Workflow engine (thiết kế), CommentsPost/Likes (cổng public), HDV thu-chi/bàn giao + hotel room-class (niche, cần user ưu tiên).

**✅ KPI phễu (main `4911574`):** `/reports/kpi` — báo giá→chấp nhận(Status=2)→chuyển đơn(ConvertedOrderId)→thu tiền, kèm tỉ lệ chấp nhận/chuyển đơn/thu + giá trị đơn TB. Từ dữ liệu sẵn (Quote/Order/Receipt), `OrderMath.Rate` chia-0 an toàn. Khác dashboard (doanh thu/công nợ) — bổ sung chỉ số phễu/chuyển đổi. **19 feature phiên tối 2026-07-11.** Test: 310 unit + 91 integration + 4 arch + 105 web.

**✅ CMS bài viết (main `3e3b570`):** Post + PostCategory (legacy Posts/CategoriesPost) — nội dung/tin tức, slug duy nhất/tenant, PublishedAt tự set khi xuất bản, lọc chuyên mục/trạng thái, quyền post.* (Content). Groundable không cần external. **→ ĐÃ HẾT mọi thứ dựng được không cần external/design.** Còn lại tuyệt đối cần user: impl provider SMS/Zalo/CRM/BankHub prod (API key + contract), Workflow engine động (thiết kế), CommentsPost/Likes (cần cổng public), KPI (đã phủ phần lớn bởi dashboard + commission-by-user + turnover-by-department). **Tổng phiên tối 2026-07-11: 18 feature merge**, test 234→308 unit / 78→90 integration / 63→104 web.

**✅ Đa kênh marketing qua abstraction (main `0019413`):** Email/SMS/Zalo đều gửi qua abstraction (`IEmailSender` có SMTP thật; `ISmsSender`+`LogSmsSender`, `IZaloSender`+`LogZaloSender` — dev log chạy ngay, prod drop-in provider đọc `{Sms,Zalo}:Provider`). CampaignService switch theo kênh + `TrySendAsync` bền per-recipient (lỗi→Status=Failed). **HẾT phần groundable qua pattern dev-log/prod-config.** Còn lại THẬT SỰ cần external: impl provider SMS/Zalo prod cụ thể (API key + chọn NCC), BankHub (bank API), CRM/eTour sync (endpoint+key); low-value/design: CMS blog, Workflow engine động, KPI. **Tổng phiên tối 2026-07-11: 17 feature merge.** Test: 301 unit + 90 integration + 4 arch + 102 web.

**✅ Subsystem nội bộ 🔴-nhưng-groundable (main `2e00dd3`):** Phát hiện không phải mọi 🔴 cần credential — 3 mục tự chứa đã làm: **WorkTask/Tasking** (`2c6059d` — giao/theo dõi việc, lọc, quyền task.*), **Notification in-app** (`99b6700` — thông báo cá nhân + chuông header đếm chưa đọc + nối Tasking→Notification khi giao việc; chỉ cần [Authorize], cách ly user), **Email campaign** (`2e00dd3` — CampaignService kênh Email gửi thật qua IEmailSender, dev log/prod SMTP, bền per-recipient). **CÒN LẠI thật sự cần external:** SMS/Zalo gateway (API provider), BankHub (bank API), CRM/eTour sync (endpoint+key); và low-value/design: CMS blog (ngoài scope tour), Workflow engine động + KPI (cần thiết kế/định nghĩa metric). Test: 300 unit + 90 integration + 4 arch + 102 web.

**✅ NHÓM 🟡 GROUNDABLE CẠN HẾT (main `22d67d9`):** Sau report-by-department làm nốt 2 mục cuối: **Currency/ExchangeRate** (`0f1fed3` — tỷ giá + ProviderService.CurrencyCode, quy đổi VND tập trung ở ProviderServiceService, picker OrderCost/Quote dùng VND; cost khoá VND khi chọn) + **PaymentTerm/ServicePaymentTerm** (`22d67d9` — catalog điều khoản TT NCC + Provider.PaymentTermId). Toàn bộ file gap `docs/business/legacy-feature-gap-analysis.md` nhóm 🟡 ĐÃ ✅. **Còn lại CHỈ 🔴** (SMS/Zalo/Email gateway, Tasking/Workflow/KPI, CMS/blog, BankHub) — đều cần API/credential ngoài hoặc subsystem lớn ngoài phạm vi. Tổng phiên tối 2026-07-11: **12 feature** merge liên tiếp, mỗi cái test+migration xanh. Test: 282 unit + 90 integration + 4 arch + 98 web.

**✅ Báo cáo doanh thu theo phòng ban (main `e3ac6d5`):** Tận dụng Department vừa làm — gom đơn theo phòng ban của sales (Order.SalesUserId → User.DepartmentId), đơn không sales/chưa gán → "Chưa phân bổ". `GetTurnoverByDepartmentAsync` + endpoint `/reports/turnover-by-department` (perm report.turnover.view sẵn có) + FE. 1 integration test gán-user-vào-phòng-ban end-to-end. Groundable-không-cần-quyết-định đã CẠN. Còn lại: ExchangeRate/ServicePaymentTerm (cần chốt điểm dùng tài chính — KHÔNG có schema legacy), nhóm 🔴 (SMS/Zalo/BankHub cần API/credential).

**✅ Phụ thu theo đơn (main `76852ee`):** Surcharge catalog (Fixed/Percent) + OrderSurcharge line cộng thẳng vào `Order.TotalRevenue` → tự chảy vào công nợ/hoa hồng/báo cáo (KHÔNG sửa downstream). Bất biến tài chính: `TotalRevenue = gốc + Σ Amount` → % luôn tính trên gốc (`base = TotalRevenue − Σ Amount`), xoá trừ đúng Amount lưu sẵn. `OrderMath.SurchargeAmount` 1 chỗ. Panel trên chi tiết đơn (invalidate ['orders'] để doanh thu cập nhật) + trang catalog. Quyền `booking.*`. 272 unit + 89 integration + 4 arch + 92 web. Còn 🟡: ExchangeRate/ServicePaymentTerm (cần chốt điểm dùng), report theo phòng ban (groundable từ Department).

**✅ Loạt catalog + cơ cấu tổ chức (main `603697a`, phiên 2026-07-11 tối, nối tiếp):** Sau PaymentAccount làm thêm: **CarType** (loại xe keyed số ghế, `vehicle.*`), **LanguageType** (ngôn ngữ HDV, `guide.*`), **Department+Position+Users** (cơ cấu tổ chức — User += DepartmentId/PositionId nullable; impact analysis báo User CRITICAL 394 symbol nhưng additive nên 89 integration auth vẫn xanh; user chốt "làm đầy đủ gắn vào User"; trang Users gán inline; perm `user.*` nhóm Admin; UserAdminService chỉ list+gán, không tạo/xoá user). `CheckboxField` shared. **Đã cạn catalog thuần 🟡.** Bỏ LoaiDonHang/TrangThaiDonHang (hệ mới dùng enum Order.Status). Còn lại đều cần chốt điểm dùng: **ExchangeRate/Surcharge/ServicePaymentTerm KHÔNG có trong script.sql** (chỉ ở code legacy) → cần requirement thật; Department/Position report-by-phòng-ban (cần thêm report). 260 unit + 89 integration + 4 arch + 90 web xanh.

**✅ Catalog & phân tích gap (main `7aee52e`, phiên 2026-07-11 tối):** User tự làm tay: VehicleAssignment (phân xe cho chuyến, Đợt 6) + CustomerType/Source/Tag + doc `docs/business/legacy-feature-gap-analysis.md` (đối chiếu đầy đủ 127 bảng script.sql). Claude verify sạch rồi merge. Sau đó Claude làm tiếp theo ưu tiên 🟡 của doc gap: **PaymentAccount** (tài khoản nhận tiền, legacy PaymentMethod — quy tắc 1 IsDefault/tenant, perm `paymentaccount.*` nhóm Finance, `CheckboxField` mới ở shared) + in tài khoản mặc định lên QuotePrintPage. Mẫu catalog chuẩn: entity+config+service+validator+controller+FE+test (~1 commit/catalog). **Còn theo ưu tiên 🟡:** Department/Position (đụng User — impact trước), LoaiDonHang/TrangThaiDonHang, CarType, LanguagesType. Provisioning + AuthTestFactory tự cấp mọi perm mới cho Admin role.

**✅ Tour lẻ FIT (main `7087d25`):** FIT = chuyến RIÊNG không template (nghiệp vụ chốt theo uỷ quyền chuyên gia). Chuyển đơn từ báo giá: bỏ trống chuyến + nhập ngày → tự tạo chuyến `FIT-<mã>` TotalSlots = đúng số khách (chuyến kín) qua IDepartureService. Kỹ thuật then chốt: `SeatPrices` override trong `IBookingService.CreateBookingAsync` — giá chỗ khi chuyển từ báo giá (cả 2 chế độ) = giá 3 hạng của QUOTE (QuoteMath), template chỉ bắt buộc khi không override; FIT không hỗ trợ giữ chỗ (hold cần ReservationHours của template). Còn "đặt hotel/vé/visa nâng cao" (chi tiết phòng/bay/hộ chiếu) vẫn cần requirement.

**✅ CỤM BAOGIA/DUTRU HOÀN TẤT (main `f242410`):** 5 mảnh liên tiếp: (1) dự trù giá `19acbab`, (2) chuyển đơn `485d9cb`, (3) HoldReminderJob `97eedde`, (4) bản in `/quotes/:id/print` `e785dfc` (chỉ giá bán, không in vốn/lãi; thay template .docx legacy), (5) nối flow thu tiền `f242410` — đối chiếu legacy BillPaymentRequest = PaymentVoucher+duyệt → hệ mới ĐÃ CÓ (Receipt/ReceiptApproval/PaymentApproval), chỉ thêm nút "Đơn" trên báo giá đã chuyển. Vòng khép kín: dự trù → báo giá → in → chấp nhận → chuyển đơn (chống overbooking, doanh thu = giá chốt) → thu tiền/duyệt → job nhắc.

**✅ HoldReminderJob (`97eedde` main):** nhắc hạn giữ chỗ — `Seat(TourCustomer).HoldExpiresAt` có sẵn (trước tưởng thiếu field vì chỉ check Order/TourDeparture). Chỗ giữ chưa cọc/chưa huỷ còn ≤24h → email sales (Order.SalesUserId); idempotent `HoldReminderSentAt`; per-tenant pattern. Cron "15 * * * *".

**✅ CareReminderJob (Đợt 7, sau merge):** Job Hangfire nghiệp vụ đầu tiên — quét `CustomerCare` tới hạn `RemindAt` chưa gửi → email người phụ trách qua `IEmailSender` (dev log, prod SMTP), đánh dấu `ReminderSentAt` (idempotency). **Pattern đa-tenant chuẩn:** `IgnoreQueryFilters()` để quét mọi tenant + xử lý PER-TENANT (set `AmbientTenantContext.SetTenant(tenantId)` trước mỗi `SaveChanges`) để qua guard chống-ghi-chéo-tenant ở `AppDbContext.ApplyTenantAndTimestamps`. Đã merge thẳng vào `main` (`73c7103`), +col `CustomerCare.ReminderSentAt` + migration, 2 test job. Job "nhắc hạn giữ chỗ" + BankHub vẫn cần input.

**✅ ĐÃ MERGE TOÀN BỘ 19 PR (#2–#20) VÀO `main` (2026-07-11).** Cách làm: nhánh `integration/merge-all` off main, merge tuần tự với union merge-driver (`.git/info/attributes`) cho các file danh sách bồi (`Program.cs`, `AppDbContext.cs`, `Permissions.cs`, `router.tsx`, `AppShell.tsx`, snapshot); rồi **gộp 14 migration dev thành 1 `AddMergedFeatures`** (khôi phục snapshot về baseline AddVehicle → re-scaffold) — an toàn vì SQLite dev tạo mới khi khởi động, không có prod data. Verify: build Release 0 warning; **4 arch + 201 unit + 81 integration + 73 web** xanh; `dotnet ef database update` chạy sạch trên SQLite mới (đủ bảng). main đã push (`c5cf291`), cả 19 PR GitHub báo merged. Remote feat branches còn (classifier chặn mass-delete — user tự xoá nếu muốn); local đã dọn chỉ còn `main`.

**Vừa làm:** Đợt 6 — phân công HDV (`TourGuideAssignment`) backend + frontend Xe/HDV. Ở nhánh `feat/dot6-guide-assignment`, **PR #2** (https://github.com/trieuthang1991/tourkit-crm/pull/2), chưa merge. Test: 154 unit + 78 integration + 4 arch + 63 web xanh.

**Đã làm thêm (phiên tự động /goal) — 12 PR, chưa merge, mỗi PR 1 nhánh riêng:**
- **#2** guide assignment + Xe/HDV FE · **#3** audit log · **#4** Calendar · **#5** duyệt chi nhiều cấp · **#6** Customer fields · **#7** đính kèm tệp IFileStorage · **#8** hoa hồng theo loại khách · **#9** quỹ vé TicketFund · **#10** Báo giá (Quote, thiết kế) · **#11** Hoá đơn VAT (Invoice, thiết kế) · **#12** Đặt dịch vụ lẻ (ServiceBooking hotel/vé/visa, thiết kế) · **#13** frontend ServiceBookings.
→ Đã phủ TOÀN BỘ subsystem dữ liệu/API của roadmap (kể cả các mục "cần chốt nghiệp vụ" — Báo giá/VAT/hotel-visa được THIẾT KẾ chuẩn vì legacy không có model). Tất cả tested (unit+integration+arch+web xanh), nhánh riêng.
- **#14** B2B Agent Portal MVP backend đầy đủ (Agent + AgentQuoteRequest workflow Requested→Quoted→Confirmed/Rejected + AgentBooking từ quote Confirmed + AgentPassenger) — bám tài liệu §4.2.1-4.2.5.

**→ ĐÃ HOÀN THÀNH TOÀN BỘ BACKEND (data/API) cho cả roadmap legacy-gap LẪN B2B Portal MVP core.** 13 PR, mỗi PR nhánh riêng, tất cả tested (159 unit + 78 integration + 4 arch + web xanh).

**Frontend cũng đã phủ (PR #13/#15/#16):** service-bookings, agents, customer-commission-rules (CRUD); **Báo giá + Hoá đơn VAT với editor dòng động** (useFieldArray); **workflow B2B báo giá** (chào giá/xác nhận/từ chối). Cùng FE cũ: guide/vehicle/audit/calendar/customer.

**→ ĐÃ XONG TOÀN BỘ phần buildable-không-cần-external: complete backend + complete frontend (mọi CRUD + aggregate line-editor + workflow UI). 19 PR (#2–#20).**

**Bổ sung phiên 2026-07-11 (hạ tầng chuẩn + follow-up groundable):**
- **#17** đính kèm tệp (IFileStorage) · **#18** Hangfire background-jobs infra (in-memory, testhost-guard, heartbeat) · **#19** Email sender abstraction (LogEmailSender dev / SmtpEmailSender prod chọn qua `Email:Provider`, mô hình IFileStorage) · **#20** liên kết `OrderCost.ProviderServiceId` → chọn giá bảng giá NCC (Đợt 5 follow-up dòng 68, validate thuộc đúng NCC). Tất cả tested (4 arch + 150 unit + 78 integration + 59 web xanh).

Còn lại — HARD-BLOCKED, chỉ user cung cấp được (KHÔNG có cách extract legacy/pattern):
- **Credential/endpoint thật:** SMTP thật để bật gửi (abstraction #19 đã sẵn, chỉ điền config), CRM/eTour API (§4.2.10), BankHub ngân hàng.
- **Quyết định nghiệp vụ:** job Hangfire cụ thể (nhắc hạn/CSKH — cần chốt job nào + tích hợp email + pattern multi-tenant IgnoreQueryFilters).
- **Thiết kế collab (roadmap "cần chốt nghiệp vụ với chủ dự án"):** DuTruTours + BaoGia/SingleTour/BookingHotel/Visa/InvoiceBranch (hệ con lớn, DuTru entangle BaoGia_Id/DuyetBooking/ArrChildService — không map sạch sang Quote/Order đã build); duyệt chi nhiều cấp (cần quyết số cấp); B2B Phase 2/3 (Dynamic Quote Builder, Approval Workflow).
- Trivial còn sót: FE ticket-fund (isClosed bool), FE agent-booking từ quote Confirmed + passenger.

**Đã EXHAUST mọi thứ có model legacy sạch / spec quyết. VERIFIED: các subsystem lớn KHÔNG có model EF legacy sạch** (Báo giá = chỉ template .docx; hotel/air/visa booking, VAT invoice, SingleTour = không có model) → **buộc phải thiết kế** (roadmap ghi "cần chốt nghiệp vụ với chủ dự án"). Legacy model sạch còn lại chỉ là lookup niche (ClassHotel/HotelType) hoặc BookingTicket (scope mơ hồ, roadmap flag cần chốt). Còn lại đều cần input:

**Còn lại (theo `docs/business/legacy-gap-roadmap.md`):**
- **Cần chốt nghiệp vụ với chủ dự án:** Đợt 5 deferred (Báo giá/BaoGia, Tour lẻ FIT, đặt hotel/vé/visa, hoá đơn VAT); Đợt 6 sau (Calendar UI, quỹ vé HDV/TicketFund, ký giờ SignatureGuide, thu-chi hộ HDV); hoa hồng theo loại khách + chốt sổ; duyệt chi nhiều cấp.
- **Hạ tầng (Đợt 7, làm khi cần):** BackgroundJobs (Hangfire), Upload (IFileStorage), BankHub.
- **B2B Agent Portal** — stream MỚI lớn (`docs/B2B_Agent_Portal_TaiLieuYeuCau_VN.md`), mới có requirement, CHƯA có thiết kế kỹ thuật → cần brainstorm/design trước.

Xem [[project-layout-and-tooling]], [[business-logic-follow-old-project]].
