# Phase 0b-2 — RBAC (Role/Permission) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.
>
> **BẮT BUỘC ĐỌC TRƯỚC:** `docs/conventions/backend-conventions.md` (§4 tenancy, §5 EF, §6 API, §8 bảo mật) và `docs/business/database-optimization-analysis.md` §J (catalog quyền thật + nguyên tắc tách trục quyền-hành-động vs data-scope). Enforcement ép net9, nullable, warnings-as-errors, braces, sealed, file-scoped ns.

**Goal:** Endpoint nghiệp vụ được gác theo **quyền hạt** (`resource.action`). Người dùng có role → role có permission → login nhét mã quyền vào JWT claim `perm` → policy chặn/ cho phép từng endpoint. Dữ liệu RBAC cô lập theo tenant; catalog `Permission` là global (seed sẵn).

**Architecture:** `Permission` là bảng **global** (BaseEntity, KHÔNG tenant) seed từ một catalog mã quyền cố định trong code. `Role`, `RolePermission`, `UserRole` là **ITenantEntity** (mỗi tenant tự định nghĩa role & gán). Khi login/refresh, `AuthService` nạp danh sách mã quyền của user (UserRole→RolePermission→Permission.Code) và `JwtTokenService` thêm mỗi mã thành 1 claim `perm`. `Program` đăng ký 1 authorization policy cho mỗi mã quyền (`RequireClaim("perm", code)`); endpoint dùng `.RequireAuthorization(code)`. `AuthTestFactory` (test) seed 1 role "Admin" đủ quyền cho user seed → test cũ vẫn xanh; thêm test user thiếu quyền → 403.

**Tech Stack:** .NET 9, EF Core 9, ASP.NET Core Authorization policies, JWT (đã có 0b-1). Trục **data-scope** (Own/Department/Branch) tách riêng, **ngoài phạm vi 0b-2** (chờ có Department/Branch — §J ghi rõ chỉ nên là scope, không nhét vào permission).

**Phạm vi vs sau:** 0b-2 = permission-gating cho endpoint hiện có (customers, tour-templates). Defer: data-scope, quản trị role/permission qua API (CRUD role — 0b-3 provisioning hoặc admin module), UI.

---

## File Structure

```
src/TourKit.Infrastructure/
  Entities/Permission.cs               # NEW — BaseEntity (global, KHÔNG ITenantEntity): Code[unique], Group
  Entities/Role.cs                     # NEW — ITenantEntity: Name
  Entities/RolePermission.cs           # NEW — ITenantEntity: RoleId, PermissionId
  Entities/UserRole.cs                 # NEW — ITenantEntity: UserId, RoleId
  Persistence/AppDbContext.cs          # MODIFY — 4 DbSet
  Persistence/Configurations/{Permission,Role,RolePermission,UserRole}Configuration.cs  # NEW
  Migrations/*_AddRbac.cs              # NEW
src/TourKit.Api/
  Authz/Permissions.cs                 # NEW — hằng mã quyền + catalog All (code, group)
  Authz/PermissionSeeder.cs            # NEW — upsert global Permission từ catalog (idempotent)
  Auth/JwtTokenService.cs              # MODIFY — CreateAccessToken(user, IEnumerable<string> perms)
  Auth/IJwtTokenService.cs             # MODIFY — chữ ký mới
  Auth/AuthService.cs                  # MODIFY — nạp perms khi login/refresh
  Program.cs                           # MODIFY — seed permissions + đăng ký policy mỗi mã + gate endpoint
  Customers/CustomerEndpoints.cs       # MODIFY — .RequireAuthorization(code) mỗi endpoint
  Catalog/TourTemplateEndpoints.cs     # MODIFY — .RequireAuthorization(code) mỗi endpoint
tests/TourKit.Tests/
  Support/AuthTestFactory.cs           # MODIFY — seed Admin role đủ quyền; thêm overload seed user KHÔNG quyền
  Auth/JwtTokenServiceTests.cs         # MODIFY — chữ ký CreateAccessToken mới
  Authz/PermissionAuthorizationTests.cs# NEW — có quyền → OK; thiếu quyền → 403; token mang perm
```

**Nguyên tắc quyết định:** `Permission` global (seed 1 lần) vì mã quyền là hằng của nền tảng — không để mỗi tenant tự định nghĩa mã, chỉ tự gán role. Policy-per-code đăng ký sẵn ở startup (đơn giản, không cần custom policy provider). Trục data-scope tách riêng (§J) — không làm ở 0b-2.

---

### Task 1: Catalog mã quyền + entities RBAC + config + migration

**Files:**
- Create: `src/TourKit.Api/Authz/Permissions.cs`
- Create: `Entities/Permission.cs`, `Role.cs`, `RolePermission.cs`, `UserRole.cs`
- Create: 4 config trong `Persistence/Configurations/`
- Modify: `Persistence/AppDbContext.cs`

- [ ] **Step 1: Catalog mã quyền**

Create `src/TourKit.Api/Authz/Permissions.cs`:

```csharp
namespace TourKit.Api.Authz;

/// <summary>Catalog mã quyền nền tảng (global). Map từ hệ cũ — xem database-optimization-analysis §J.</summary>
public static class Permissions
{
    public const string CustomerView = "customer.view";
    public const string CustomerCreate = "customer.create";
    public const string CustomerUpdate = "customer.update";
    public const string CustomerDelete = "customer.delete";

    public const string TourView = "tour.view";
    public const string TourCreate = "tour.create";
    public const string TourUpdate = "tour.update";
    public const string TourDelete = "tour.delete";

    /// <summary>Toàn bộ mã quyền + nhóm hiển thị. Dùng để seed + đăng ký policy.</summary>
    public static readonly IReadOnlyList<(string Code, string Group)> All =
    [
        (CustomerView, "Customer"), (CustomerCreate, "Customer"),
        (CustomerUpdate, "Customer"), (CustomerDelete, "Customer"),
        (TourView, "Catalog"), (TourCreate, "Catalog"),
        (TourUpdate, "Catalog"), (TourDelete, "Catalog"),
    ];
}
```

- [ ] **Step 2: `Permission` (global)**

Create `src/TourKit.Infrastructure/Entities/Permission.cs`:

```csharp
using TourKit.Shared.Entities;

namespace TourKit.Infrastructure.Entities;

/// <summary>Quyền hạt nền tảng (global — KHÔNG thuộc tenant). Seed từ catalog code.</summary>
public sealed class Permission : BaseEntity
{
    public string Code { get; set; } = string.Empty;
    public string Group { get; set; } = string.Empty;
}
```

- [ ] **Step 3: `Role` / `RolePermission` / `UserRole` (tenant)**

Create `src/TourKit.Infrastructure/Entities/Role.cs`:

```csharp
using TourKit.Shared.Entities;

namespace TourKit.Infrastructure.Entities;

public sealed class Role : BaseEntity, ITenantEntity
{
    public Guid TenantId { get; set; }
    public string Name { get; set; } = string.Empty;
}
```

Create `src/TourKit.Infrastructure/Entities/RolePermission.cs`:

```csharp
using TourKit.Shared.Entities;

namespace TourKit.Infrastructure.Entities;

public sealed class RolePermission : BaseEntity, ITenantEntity
{
    public Guid TenantId { get; set; }
    public Guid RoleId { get; set; }
    public Guid PermissionId { get; set; }
}
```

Create `src/TourKit.Infrastructure/Entities/UserRole.cs`:

```csharp
using TourKit.Shared.Entities;

namespace TourKit.Infrastructure.Entities;

public sealed class UserRole : BaseEntity, ITenantEntity
{
    public Guid TenantId { get; set; }
    public Guid UserId { get; set; }
    public Guid RoleId { get; set; }
}
```

- [ ] **Step 4: Config**

Create `src/TourKit.Infrastructure/Persistence/Configurations/PermissionConfiguration.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using TourKit.Infrastructure.Entities;

namespace TourKit.Infrastructure.Persistence.Configurations;

public sealed class PermissionConfiguration : IEntityTypeConfiguration<Permission>
{
    public void Configure(EntityTypeBuilder<Permission> builder)
    {
        builder.Property(x => x.Code).IsRequired().HasMaxLength(100);
        builder.Property(x => x.Group).IsRequired().HasMaxLength(50);
        builder.HasIndex(x => x.Code).IsUnique();       // global unique
    }
}
```

Create `src/TourKit.Infrastructure/Persistence/Configurations/RoleConfiguration.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using TourKit.Infrastructure.Entities;

namespace TourKit.Infrastructure.Persistence.Configurations;

public sealed class RoleConfiguration : IEntityTypeConfiguration<Role>
{
    public void Configure(EntityTypeBuilder<Role> builder)
    {
        builder.Property(x => x.Name).IsRequired().HasMaxLength(100);
        builder.HasIndex(x => new { x.TenantId, x.Name }).IsUnique();
    }
}
```

Create `src/TourKit.Infrastructure/Persistence/Configurations/RolePermissionConfiguration.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using TourKit.Infrastructure.Entities;

namespace TourKit.Infrastructure.Persistence.Configurations;

public sealed class RolePermissionConfiguration : IEntityTypeConfiguration<RolePermission>
{
    public void Configure(EntityTypeBuilder<RolePermission> builder)
    {
        builder.HasIndex(x => new { x.TenantId, x.RoleId, x.PermissionId }).IsUnique();
    }
}
```

Create `src/TourKit.Infrastructure/Persistence/Configurations/UserRoleConfiguration.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using TourKit.Infrastructure.Entities;

namespace TourKit.Infrastructure.Persistence.Configurations;

public sealed class UserRoleConfiguration : IEntityTypeConfiguration<UserRole>
{
    public void Configure(EntityTypeBuilder<UserRole> builder)
    {
        builder.HasIndex(x => new { x.TenantId, x.UserId, x.RoleId }).IsUnique();
    }
}
```

- [ ] **Step 5: DbSet** — thêm vào `AppDbContext` dưới `TourItineraries`:

```csharp
    public DbSet<Permission> Permissions => Set<Permission>();
    public DbSet<Role> Roles => Set<Role>();
    public DbSet<RolePermission> RolePermissions => Set<RolePermission>();
    public DbSet<UserRole> UserRoles => Set<UserRole>();
```

(Filter loop hiện tại đã đúng: `Permission` là BaseEntity không-tenant → nhận soft-delete filter; `Role/RolePermission/UserRole` là ITenantEntity gốc → nhận tenant filter. Không cần sửa loop.)

- [ ] **Step 6: Build + migration**

```bash
dotnet build
export PATH="$PATH:$HOME/.dotnet/tools"
dotnet ef migrations add AddRbac --project src/TourKit.Infrastructure --startup-project src/TourKit.Api
dotnet ef database update --project src/TourKit.Infrastructure --startup-project src/TourKit.Api
```
Expected: tạo `Permissions` (unique Code), `Roles`, `RolePermissions`, `UserRoles`. Build 0/0. `Done.`

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(rbac): Permission (global) + Role/RolePermission/UserRole + catalog quyền + migration"
```

---

### Task 2: Seed permission global khi startup

**Files:**
- Create: `src/TourKit.Api/Authz/PermissionSeeder.cs`
- Modify: `src/TourKit.Api/Program.cs`
- Test: `tests/TourKit.Tests/Authz/PermissionAuthorizationTests.cs` (tạo file, test seeding trước)

- [ ] **Step 1: `PermissionSeeder`**

Create `src/TourKit.Api/Authz/PermissionSeeder.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using TourKit.Infrastructure.Entities;
using TourKit.Infrastructure.Persistence;

namespace TourKit.Api.Authz;

/// <summary>Upsert catalog quyền vào bảng global Permission (idempotent). Chạy khi startup.</summary>
public static class PermissionSeeder
{
    public static async Task SeedAsync(AppDbContext db, CancellationToken ct = default)
    {
        var existing = await db.Permissions.Select(p => p.Code).ToListAsync(ct);
        var missing = Permissions.All.Where(c => !existing.Contains(c.Code));
        foreach (var (code, group) in missing)
        {
            db.Permissions.Add(new Permission { Code = code, Group = group });
        }

        await db.SaveChangesAsync(ct);
    }
}
```

- [ ] **Step 2: Gọi seed sau `builder.Build()` trong `Program.cs`**

Thêm ngay sau `var app = builder.Build();` (trước `app.UseExceptionHandler();`):

```csharp
using (var scope = app.Services.CreateScope())
{
    var db = scope.ServiceProvider.GetRequiredService<AppDbContext>();
    await TourKit.Api.Authz.PermissionSeeder.SeedAsync(db);
}
```

(Chạy ở cả app thật lẫn WebApplicationFactory trong test — code trước `app.Run()` được thực thi khi host khởi động.)

- [ ] **Step 3: Test seeding**

Create `tests/TourKit.Tests/Authz/PermissionAuthorizationTests.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.DependencyInjection;
using TourKit.Api.Authz;
using TourKit.Infrastructure.Persistence;
using TourKit.Tests.Support;

namespace TourKit.Tests.Authz;

public class PermissionAuthorizationTests : IClassFixture<AuthTestFactory>
{
    private readonly AuthTestFactory _factory;

    public PermissionAuthorizationTests(AuthTestFactory factory) => _factory = factory;

    [Fact]
    public async Task Global_permissions_are_seeded()
    {
        using var scope = _factory.Services.CreateScope();
        var db = scope.ServiceProvider.GetRequiredService<AppDbContext>();
        var codes = await db.Permissions.Select(p => p.Code).ToListAsync();

        Assert.Contains(Permissions.TourCreate, codes);
        Assert.Equal(Permissions.All.Count, codes.Distinct().Count());
    }
}
```

- [ ] **Step 4: Build + test**

Run: `dotnet test --filter Global_permissions_are_seeded`
Expected: Passed! 1 test. Build 0/0.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(rbac): seed catalog quyền global khi startup + test"
```

---

### Task 3: Nạp quyền vào JWT (AuthService + JwtTokenService)

**Files:**
- Modify: `src/TourKit.Api/Auth/IJwtTokenService.cs`, `Auth/JwtTokenService.cs`, `Auth/AuthService.cs`
- Modify: `tests/TourKit.Tests/Auth/JwtTokenServiceTests.cs`

- [ ] **Step 1: Đổi chữ ký `IJwtTokenService.CreateAccessToken`**

Trong `src/TourKit.Api/Auth/IJwtTokenService.cs`:

```csharp
    string CreateAccessToken(User user, IEnumerable<string> permissions);
```

- [ ] **Step 2: Sửa `JwtTokenService.CreateAccessToken`** — thêm claim `perm` mỗi quyền

Thay method `CreateAccessToken` trong `src/TourKit.Api/Auth/JwtTokenService.cs`:

```csharp
    public string CreateAccessToken(User user, IEnumerable<string> permissions)
    {
        var creds = new SigningCredentials(
            new SymmetricSecurityKey(Encoding.UTF8.GetBytes(_opt.Secret)),
            SecurityAlgorithms.HmacSha256);

        var claims = new List<Claim>
        {
            new(JwtRegisteredClaimNames.Sub, user.Id.ToString()),
            new("tenant_id", user.TenantId.ToString()),
            new("email", user.Email),
            new(JwtRegisteredClaimNames.Jti, Guid.NewGuid().ToString()),
        };
        foreach (var code in permissions)
        {
            claims.Add(new Claim("perm", code));
        }

        var token = new JwtSecurityToken(
            issuer: _opt.Issuer,
            audience: _opt.Audience,
            claims: claims,
            expires: AccessTokenExpiry().UtcDateTime,
            signingCredentials: creds);

        return new JwtSecurityTokenHandler().WriteToken(token);
    }
```

- [ ] **Step 3: `AuthService` nạp quyền khi login + refresh**

Trong `src/TourKit.Api/Auth/AuthService.cs`, thêm helper và dùng ở `IssueAsync`. Thêm method:

```csharp
    private async Task<List<string>> LoadPermissionsAsync(Guid userId, CancellationToken ct)
    {
        // UserRole → RolePermission → Permission.Code (RBAC là ITenantEntity nên đã lọc theo tenant hiện tại).
        return await _db.UserRoles.Where(ur => ur.UserId == userId)
            .Join(_db.RolePermissions, ur => ur.RoleId, rp => rp.RoleId, (ur, rp) => rp.PermissionId)
            .Join(_db.Permissions, pid => pid, p => p.Id, (pid, p) => p.Code)
            .Distinct()
            .ToListAsync(ct);
    }
```

Sửa `IssueAsync` — dòng tạo access token thành:

```csharp
        var permissions = await LoadPermissionsAsync(user.Id, ct);
        var access = _jwt.CreateAccessToken(user, permissions);
```

(`IssueAsync` được gọi sau `_tenant.SetTenant(...)` ở cả login và refresh nên query RBAC đã đúng tenant.)

- [ ] **Step 4: Cập nhật `JwtTokenServiceTests`** — chữ ký mới + kiểm claim `perm`

Trong `tests/TourKit.Tests/Auth/JwtTokenServiceTests.cs`, sửa test `Access_token_carries_tenant_and_user_claims`: đổi `svc.CreateAccessToken(user)` → `svc.CreateAccessToken(user, ["tour.create", "customer.view"])`, và thêm assert:

```csharp
        var perms = jwt.Claims.Where(c => c.Type == "perm").Select(c => c.Value).ToList();
        Assert.Contains("tour.create", perms);
        Assert.Contains("customer.view", perms);
```

(Giữ nguyên các assert sub/tenant_id/email cũ.)

- [ ] **Step 5: Build + test**

Run: `dotnet test --filter JwtTokenServiceTests`
Expected: Passed! (test đã cập nhật). Build 0/0. Cả suite chưa chạy endpoint gating nên vẫn xanh.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(rbac): login/refresh nạp quyền user vào claim perm của JWT"
```

---

### Task 4: Policy theo quyền + gate endpoint + seed Admin role trong test

**Files:**
- Modify: `src/TourKit.Api/Program.cs`
- Modify: `src/TourKit.Api/Customers/CustomerEndpoints.cs`, `Catalog/TourTemplateEndpoints.cs`
- Modify: `tests/TourKit.Tests/Support/AuthTestFactory.cs`

- [ ] **Step 1: Đăng ký policy mỗi mã quyền** trong `Program.cs`

Thay `builder.Services.AddAuthorization();` bằng:

```csharp
builder.Services.AddAuthorization(options =>
{
    foreach (var (code, _) in TourKit.Api.Authz.Permissions.All)
    {
        options.AddPolicy(code, policy => policy.RequireClaim("perm", code));
    }
});
```

- [ ] **Step 2: Gate endpoint customers** — mỗi endpoint 1 quyền. Trong `src/TourKit.Api/Customers/CustomerEndpoints.cs`, đổi group về chỉ nhóm (bỏ `.RequireAuthorization()` trần) rồi gắn policy từng route. Sửa dòng group + thêm `.RequireAuthorization(code)` cho từng map. Ví dụ (áp cho cả 5 route):

```csharp
        var group = app.MapGroup("/api/v1/customers");

        group.MapGet("/", /* ... */).RequireAuthorization(Authz.Permissions.CustomerView);
        group.MapGet("/{id:guid}", /* ... */).RequireAuthorization(Authz.Permissions.CustomerView);
        group.MapPost("/", /* ... */).RequireAuthorization(Authz.Permissions.CustomerCreate);
        group.MapPut("/{id:guid}", /* ... */).RequireAuthorization(Authz.Permissions.CustomerUpdate);
        group.MapDelete("/{id:guid}", /* ... */).RequireAuthorization(Authz.Permissions.CustomerDelete);
```

Thêm `using TourKit.Api.Authz;` nếu cần (hoặc dùng đủ namespace như trên). Giữ nguyên thân handler.

- [ ] **Step 3: Gate endpoint tour-templates** — tương tự trong `Catalog/TourTemplateEndpoints.cs`:

```csharp
        var group = app.MapGroup("/api/v1/tour-templates");

        group.MapGet("/", /* ... */).RequireAuthorization(Authz.Permissions.TourView);
        group.MapGet("/{id:guid}", /* ... */).RequireAuthorization(Authz.Permissions.TourView);
        group.MapPost("/", /* ... */).RequireAuthorization(Authz.Permissions.TourCreate);
        group.MapPut("/{id:guid}", /* ... */).RequireAuthorization(Authz.Permissions.TourUpdate);
        group.MapDelete("/{id:guid}", /* ... */).RequireAuthorization(Authz.Permissions.TourDelete);
        group.MapGet("/{id:guid}/itinerary", /* ... */).RequireAuthorization(Authz.Permissions.TourView);
        group.MapPut("/{id:guid}/itinerary", /* ... */).RequireAuthorization(Authz.Permissions.TourUpdate);
```

Thêm `using TourKit.Api.Authz;`.

- [ ] **Step 4: `AuthTestFactory` seed Admin role đủ quyền** — để test cũ vẫn qua gate

Trong `tests/TourKit.Tests/Support/AuthTestFactory.cs`, sửa `SeedTenantUserAsync` để sau khi tạo user thì tạo role "Admin" gán **tất cả** Permission và gán role cho user. Thêm (sau `await db.SaveChangesAsync();` tạo user, trước `return`):

```csharp
        // Admin role: gán toàn bộ permission global cho user seed (test cũ cần quyền để qua gate).
        var role = new Role { Name = "Admin" };
        db.Roles.Add(role);
        await db.SaveChangesAsync();

        var allPermIds = await db.Permissions.Select(p => p.Id).ToListAsync();
        foreach (var pid in allPermIds)
        {
            db.RolePermissions.Add(new RolePermission { RoleId = role.Id, PermissionId = pid });
        }

        db.UserRoles.Add(new UserRole { UserId = <userVar>.Id, RoleId = role.Id });
        await db.SaveChangesAsync();
```

Lưu ý: thay `<userVar>` bằng biến user đã tạo trong method (đọc code hiện tại để lấy đúng tên biến). Thêm `using`/`Microsoft.EntityFrameworkCore` nếu thiếu (`ToListAsync`). `tenantCtx.SetTenant` đã gọi trước đó nên Role/RolePermission/UserRole được gán đúng tenant qua interceptor.

- [ ] **Step 5: Build + chạy toàn bộ test cũ — kỳ vọng vẫn PASS**

Run: `dotnet test`
Expected: tất cả test cũ (customers, tour-templates, auth) vẫn xanh vì user seed giờ có đủ quyền. Build 0/0.
(Nếu 403 xuất hiện → user seed chưa được gán quyền đúng; kiểm lại Step 4.)

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(rbac): policy theo mã quyền + gate endpoint customers/tour-templates + seed Admin role trong test"
```

---

### Task 5: Test phân quyền — có quyền OK, thiếu quyền 403

**Files:**
- Modify: `tests/TourKit.Tests/Support/AuthTestFactory.cs` (thêm overload seed user hạn chế quyền)
- Modify: `tests/TourKit.Tests/Authz/PermissionAuthorizationTests.cs`

- [ ] **Step 1: Overload seed user với tập quyền tùy chọn**

Trong `AuthTestFactory.cs`, thêm method seed 1 user + role chỉ gồm các mã quyền cho trước (để test thiếu quyền). Thêm:

```csharp
    /// <summary>Seed tenant + user gán role chỉ có đúng các mã quyền cho trước (để test 403).</summary>
    public async Task<(string slug, string email, string password)> SeedTenantUserWithPermissionsAsync(
        string slug, params string[] permissionCodes)
    {
        using var scope = Services.CreateScope();
        var sp = scope.ServiceProvider;
        var db = sp.GetRequiredService<AppDbContext>();
        var hasher = sp.GetRequiredService<IPasswordHasher>();
        var tenantCtx = sp.GetRequiredService<AmbientTenantContext>();

        var tenant = new Tenant { Name = slug, Slug = slug };
        db.Tenants.Add(tenant);
        await db.SaveChangesAsync();
        tenantCtx.SetTenant(tenant.Id);

        var email = $"user@{slug}.com";
        const string password = "P@ssw0rd!";
        var user = new User { Email = email, FullName = "U", PasswordHash = hasher.Hash(password) };
        db.Users.Add(user);
        await db.SaveChangesAsync();

        var role = new Role { Name = "Limited" };
        db.Roles.Add(role);
        await db.SaveChangesAsync();

        var permIds = await db.Permissions.Where(p => permissionCodes.Contains(p.Code))
            .Select(p => p.Id).ToListAsync();
        foreach (var pid in permIds)
        {
            db.RolePermissions.Add(new RolePermission { RoleId = role.Id, PermissionId = pid });
        }

        db.UserRoles.Add(new UserRole { UserId = user.Id, RoleId = role.Id });
        await db.SaveChangesAsync();
        return (slug, email, password);
    }
```

(Bảo đảm `using TourKit.Infrastructure.Entities;` + `Microsoft.EntityFrameworkCore` đã có trong file.)

- [ ] **Step 2: Test 403 + 200 theo quyền**

Thêm vào `PermissionAuthorizationTests.cs`:

```csharp
    private async Task<HttpClient> LoginAsync((string slug, string email, string password) seed)
    {
        var client = _factory.CreateClient();
        var auth = await (await client.PostAsJsonAsync("/api/v1/auth/login",
            new TourKit.Api.Auth.LoginRequest(seed.slug, seed.email, seed.password)))
            .Content.ReadFromJsonAsync<TourKit.Api.Auth.AuthResponse>();
        client.DefaultRequestHeaders.Authorization =
            new System.Net.Http.Headers.AuthenticationHeaderValue("Bearer", auth!.AccessToken);
        return client;
    }

    [Fact]
    public async Task User_without_permission_gets_403()
    {
        // chỉ có quyền xem tour, KHÔNG có quyền tạo
        var seed = await _factory.SeedTenantUserWithPermissionsAsync("perm-a", Permissions.TourView);
        var client = await LoginAsync(seed);

        var view = await client.GetAsync("/api/v1/tour-templates");
        Assert.Equal(System.Net.HttpStatusCode.OK, view.StatusCode);

        var create = await client.PostAsJsonAsync("/api/v1/tour-templates", new
        {
            Code = "X-1", Title = "X", TourType = (string?)null, TotalSlots = 10, ReservationHours = 24,
            PriceAdult = 1m, PriceChild = 1m, PriceChildSmall = 1m, PriceBaby = 0m, TermsNote = (string?)null,
        });
        Assert.Equal(System.Net.HttpStatusCode.Forbidden, create.StatusCode);
    }
```

(Thêm `using System.Net.Http.Json;` nếu file chưa có.)

- [ ] **Step 3: Build + test**

Run: `dotnet test --filter PermissionAuthorizationTests`
Expected: Passed! 2 tests (seeded + 403/200). Build 0/0.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "test(rbac): user thiếu quyền → 403, có quyền → 200/OK"
```

---

### Task 6: Toàn bộ suite + migration verify + hoàn tất

- [ ] **Step 1: Toàn bộ test**

Run: `dotnet test`
Expected: Passed! Tất cả (0a + 0b-1 + Phase 1 + 0b-2). 0 failed, 0 skipped.

- [ ] **Step 2: Verify migration list**

```bash
export PATH="$PATH:$HOME/.dotnet/tools"
dotnet ef migrations list --project src/TourKit.Infrastructure --startup-project src/TourKit.Api
```
Expected: có `AddRbac` ở cuối.

---

## Self-Review

**Spec coverage (§J):**
- Permission global seed từ catalog → Task 1/2 ✅
- Role/RolePermission/UserRole tenant → Task 1 ✅
- Quyền vào JWT claim `perm` → Task 3 ✅
- Policy `resource.action` gate endpoint → Task 4 ✅
- Tách trục data-scope (Own/Dept/Branch) → **ngoài phạm vi** (ghi rõ, chờ có Department/Branch) ✅
- Test có/thiếu quyền → Task 5 ✅

**Placeholder scan:** Không TBD; mọi step có code/lệnh + output kỳ vọng. Chỗ `<userVar>` (Task 4 Step 4) yêu cầu implementer đọc biến user thật trong `SeedTenantUserAsync` hiện tại — đã ghi chú rõ, không phải placeholder mơ hồ.

**Type consistency:** `Permissions.{CustomerView..TourDelete, All}`, `Permission{Code,Group}`, `Role.Name`, `RolePermission{RoleId,PermissionId}`, `UserRole{UserId,RoleId}`, `IJwtTokenService.CreateAccessToken(User, IEnumerable<string>)`, `AuthService.LoadPermissionsAsync`, `AuthTestFactory.SeedTenantUserWithPermissionsAsync` — nhất quán Task 1→5. ✅

**Rủi ro đã lường:**
- Gate endpoint làm **test cũ 403** nếu user seed thiếu quyền → seed Admin role đủ quyền trong `AuthTestFactory` (Task 4 Step 4). Đây là thay đổi bắt buộc để giữ suite xanh.
- `JwtTokenService.CreateAccessToken` đổi chữ ký → cập nhật `JwtTokenServiceTests` (Task 3 Step 4).
- Seeding chạy ở startup cả trong test (WebApplicationFactory) → permissions có sẵn khi seed role.
